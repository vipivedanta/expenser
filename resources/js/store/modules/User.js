const state = {
    user : false
}

const getters = {
    getUser : state => state.user
}

const mutations = {};
const actions = {};

export default{
    state,
    getters,
    mutations,
    actions
}